<!DOCTYPE html>
<html>
    <head>
        <title>Itinerary</title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <nav>
            <a href="/" class="home-nav">home</a> 
            |
            <a href="/trips"">All Itneraries</a>
            |
            <% if (user) { %>
            <a href="/logout">Log Out</a>
            <% } else { %>
                <a href="/auth/google" class="login">Log In</a>
            <% } %>
        </nav>
